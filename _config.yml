title: Asia Tech News Feed
description: Daily Asia news digest covering semiconductors, electronics, logistics, pharma, biotech, AI & automation
baseurl: ""
url: "https://asia-tech-feed.pages.dev"

# Build settings
theme: null
plugins:
  - jekyll-feed
  - jekyll-sitemap
  - jekyll-paginate

paginate: 10
paginate_path: "/page/:num/"

# Permalink
permalink: /:year/:month/:day/:title/

# Exclude from build
exclude:
  - scripts/
  - Gemfile
  - Gemfile.lock
  - README.md
  - "*.sh"
  - vendor/

# Collections
collections:
  drafts:
    output: false

# Defaults
defaults:
  - scope:
      path: ""
      type: "posts"
    values:
      layout: "post"

markdown: kramdown
highlighter: rouge
encoding: utf-8
